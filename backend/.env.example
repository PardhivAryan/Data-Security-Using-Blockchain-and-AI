APP_NAME="Data Security Using Blockchain and AI"
DEBUG=true
API_PREFIX="/api"

CORS_ORIGINS='["http://localhost:8000"]'

DATABASE_URL="postgresql+psycopg://postgres:postgres@localhost:5432/medsec_ai"

JWT_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_SECRET"
JWT_ALG="HS256"
JWT_EXP_MINUTES=480

RP_ID="localhost"
RP_NAME="MedSec AI"
EXPECTED_ORIGIN="http://localhost:8000"

# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY="REPLACE_WITH_GENERATED_FERNET_KEY"

STORAGE_DIR="storage"
ENCRYPTED_DIR="storage/encrypted"
QUARANTINED_DIR="storage/quarantined"

RISK_MODEL_PATH="app/ml/artifacts/risk_model.pkl"